<link rel="stylesheet" href="{{ asset('styles/navbar.css') }}">
<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
    <div class="container">

        <!-- LOGO -->
<a class="navbar-brand fw-semibold d-flex align-items-center gap-2"
href="{{ path('app_home') }}">

    <!-- ICONE -->
    <img
        src="{{ asset('images/shopmanager3.ico') }}"
        alt="Logo Shop Manager"
        width="48"
        height="48"
    >

    <!-- TEXTE -->
    <span>
        Shop Manager
    </span>

</a>

        <!-- TOGGLER -->
        <button
            class="navbar-toggler"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#mainNavbar"
        >
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="mainNavbar">

            <!-- MENU GAUCHE -->
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">

                <!-- ACCUEIL -->
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_home') }}">
                        <i class="fa-solid fa-house me-1"></i> Accueil
                    </a>
                </li>

                <!-- CLIENTS -->
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                        <i class="fa-solid fa-user-group me-1"></i> Clients
                    </a>
                    <ul class="dropdown-menu">
                        <li>
                            <a class="dropdown-item" href="{{ path('app_customer_index') }}">
                                Liste des clients
                            </a>
                        </li>

                        {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_SELLER') %}
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <a class="dropdown-item" href="{{ path('app_customer_new') }}">
                                    Ajouter un client
                                </a>
                            </li>
                        {% endif %}
                    </ul>
                </li>

                <!-- FACTURES -->
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                        <i class="fa-solid fa-file-invoice-dollar me-1"></i> Factures
                    </a>
                    <ul class="dropdown-menu">
                        <li>
                            <a class="dropdown-item" href="{{ path('app_invoice_index') }}">
                                Liste des factures
                            </a>
                        </li>

                        {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_SELLER') %}
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <a class="dropdown-item" href="{{ path('app_invoice_new') }}">
                                    Ajouter une facture
                                </a>
                            </li>
                        {% endif %}
                    </ul>
                </li>

                <!-- VENDEURS -->
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                        <i class="fa-solid fa-user-tie me-1"></i> Vendeurs
                    </a>
                    <ul class="dropdown-menu">
                        <li>
                            <a class="dropdown-item" href="{{ path('app_seller_index') }}">
                                Liste des vendeurs
                            </a>
                        </li>

                        {% if is_granted('ROLE_ADMIN') %}
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <a class="dropdown-item" href="{{ path('app_seller_new') }}">
                                    Ajouter un vendeur
                                </a>
                            </li>
                        {% endif %}
                    </ul>
                </li>

                <!-- CATALOGUE -->
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                        <i class="fa-solid fa-book me-1"></i> Catalogue
                    </a>
                    <ul class="dropdown-menu">
                        <li>
                            <a class="dropdown-item" href="{{ path('app_catalog_index') }}">
                                Liste des produits
                            </a>
                        </li>

                        {% if is_granted('ROLE_ADMIN') %}
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                <a class="dropdown-item" href="{{ path('app_product_new') }}">
                                    Ajouter un produit
                                </a>
                            </li>
                        {% endif %}
                    </ul>
                </li>

                <!-- MAILPIT -->
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item ms-lg-4">
                        <a
                            class="nav-link"
                            href="#"
                            data-bs-toggle="modal"
                            data-bs-target="#mailpitModal"
                        >
                            <i class="fa-solid fa-envelope me-1"></i> Mailpit
                        </a>
                    </li>
                {% endif %}

<!-- ADMIN / STATS -->
{% if is_granted('ROLE_ADMIN') or is_granted('ROLE_SELLER') %}
    <li class="nav-item dropdown ms-lg-3">
        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
            <i class="fa-solid fa-chart-line me-1"></i>

            {% if is_granted('ROLE_ADMIN') %}
                Admin
            {% else %}
                Mes stats
            {% endif %}
        </a>

        <ul class="dropdown-menu">

            {# ADMIN UNIQUEMENT #}
            {% if is_granted('ROLE_ADMIN') %}
                <li>
                    <a class="dropdown-item" href="{{ path('app_statistics_admin') }}">
                        Tableau admin
                    </a>
                </li>
            {% endif %}

            {# SELLER UNIQUEMENT (PAS ADMIN) #}
            {% if is_granted('ROLE_SELLER') and not is_granted('ROLE_ADMIN') %}
                <li>
                    <a class="dropdown-item" href="{{ path('app_statistics_seller') }}">
                        Mes statistiques
                    </a>
                </li>
            {% endif %}

        </ul>
    </li>
{% endif %}

            </ul>

            <!-- UTILISATEUR -->
            <ul class="navbar-nav ms-auto">

                {% if app.user %}
                    <li class="nav-item">
                        <span class="nav-link text-light">
                            <i class="fa-solid fa-user me-1"></i>
                            {{ app.user.firstname }} {{ app.user.lastname }}
                        </span>
                    </li>

                    <li class="nav-item d-none d-lg-block">
                        <span class="nav-link text-secondary">|</span>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout') }}">
                            <i class="fa-solid fa-right-from-bracket me-1"></i>
                            Se déconnecter
                        </a>
                    </li>

                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_login') }}">
                            Se connecter
                        </a>
                    </li>
                {% endif %}

            </ul>

        </div>
    </div>
</nav>