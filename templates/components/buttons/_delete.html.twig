<form method="post" action="{{ path(route, {'id': id}) }}" onsubmit="return confirm('Supprimer cet élément ?');" style="display:inline;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ id) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>
</form>